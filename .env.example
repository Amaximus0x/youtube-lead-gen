# Supabase Configuration (Client-side Auth Only)
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Backend API URL
# URL of the separate youtube-scraper-backend service
PUBLIC_API_URL=http://localhost:8090

# NOTE: The following variables are NO LONGER NEEDED in the frontend:
# - SUPABASE_SERVICE_ROLE_KEY (moved to backend)
# - PROXY_URL (scraping moved to backend)
# - RATE_LIMIT_PER_MINUTE (moved to backend)
# - SCRAPER_HEADLESS (moved to backend)
# - USER_AGENT_ROTATION (moved to backend)
# - SERP_API_KEY (moved to backend)
# - SCRAPER_API_KEY (moved to backend)
# - BRIGHT_DATA_PROXY (moved to backend)
# - GOOGLE_SHEETS_* (not implemented)
#
# All backend functionality (scraping, enrichment, database access) is now
# handled by the separate youtube-scraper-backend project.
